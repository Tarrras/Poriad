package app.poruch.data.geo

/**
 * Часовий пояс за координатами.
 *
 * Подія зберігає власний пояс, а не пояс того, хто її створив: киянин, що планує зустріч у Варшаві,
 * має вказати варшавський час, і читач у Львові має побачити саме його. Досі цей пояс набирали
 * руками — рядком виду `Europe/Kyiv` у текстовому полі, посеред екрана, де все інше обирається
 * дотиком.
 *
 * Тепер його визначає місце. Кожна платформа вміє це по-своєму й з різною точністю, тому відповідь
 * необов'язкова: `null` означає «не змогли», і тоді лишається пояс пристрою — рівно те, що було
 * типовим значенням і до цієї зміни. Гірше не стає ніде, краще стає там, де платформа вміє.
 */
expect suspend fun timeZoneAt(latitude: Double, longitude: Double): String?
